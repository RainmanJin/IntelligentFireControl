<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.com.bgy.ifc.dao.system.GroupsDao">
  <resultMap id="BaseResultMap" type="cn.com.bgy.ifc.entity.po.system.Groups">
    <result column="id" jdbcType="BIGINT" property="id" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="type" jdbcType="INTEGER" property="type" />
  </resultMap>
  <insert id="insert" parameterType="cn.com.bgy.ifc.entity.po.system.Groups">
    insert into groups (id, name, type,logic_remove
      )
    values (#{id,jdbcType=BIGINT}, #{name,jdbcType=VARCHAR}, #{type,jdbcType=INTEGER},#{logicRemove,jdbcType=BIT}
      )
  </insert>
  <insert id="insertSelective" parameterType="cn.com.bgy.ifc.entity.po.system.Groups">
    insert into groups
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="id != null">
        id,
      </if>
      <if test="name != null">
        name,
      </if>
      <if test="type != null">
        type,
      </if>
      <if test="logicRemove !=null">
        logic_remove
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="id != null">
        #{id,jdbcType=BIGINT},
      </if>
      <if test="name != null">
        #{name,jdbcType=VARCHAR},
      </if>
      <if test="type != null">
        #{type,jdbcType=INTEGER},
      </if>
      <if test="logicRemove !=null">
        #{logicRemove,jdbcType=BIT}
      </if>
    </trim>
  </insert>
  <update id="deleteBatch" parameterType="java.util.List" >
    update groups  g  set g.logic_remove=1 where g.id in
    <foreach collection="ids" index="index" item="item" open="(" separator="," close=")">
      #{item}
    </foreach>
  </update>

  <select id="queryListByParam" parameterType="cn.com.bgy.ifc.entity.po.system.Groups" resultMap="BaseResultMap">
    select  id, name, type,logic_remove
      from groups
      <where>
        <trim prefixOverrides="and" >
          <if test="id != null">
            and id=#{id}
          </if>
          <if test="name != null">
            and name=#{name}
          </if>
          <if test="type != null">
            and type=#{type}
          </if>
          <if test="logicRemove !=null">
            and logic_remove=#{logicRemove}
          </if>
          <if test="keyWords !=null and keyWords !=''">
            and (name likeCONCAT('%',#{keyWords},'%')
          </if>
        </trim>
      </where>

  </select>

</mapper>